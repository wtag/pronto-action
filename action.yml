name: 'Pronto'
description: 'Run pronto for the current PR'
inputs:
  github_token:
    description: 'Token to use for reporting'
    required: true
runs:
  using: 'docker'
  image: 'docker://mokhlesur96/pronto-action:1499'
  args:
    - ${{ inputs.github_token }}
